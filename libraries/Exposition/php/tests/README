To run the unit tests, use the following:

    phpunit --configuration phpunit.xml

You can also run tests for just the controllers:

    phpunit --configuration phpunit.xml --group Exposition

or just the Exposition Widget:

    phpunit --configuration phpunit.xml --group Exposition_Widget

By default, coverage reports and TestDox are generated in the ../log/phpunitreport/
subdirectory.
